"""
Module: ui/__init__.py
Purpose: Marks the Streamlit UI package. The review/approve logic is split into
         review.py (pure, testable) and app.py (thin Streamlit shell) so the test
         suite never needs Streamlit installed.
"""
