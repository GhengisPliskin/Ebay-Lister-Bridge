[pytest]
# Run from the repo root; conftest.py adds the root to sys.path so `import src`
# resolves. The mocked suite needs NO live Gemini / eBay / Drive credentials.
testpaths = tests
python_files = test_*.py
addopts = -q
